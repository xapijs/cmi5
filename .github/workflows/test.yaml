name: Test
on:
  pull_request:
  push:
jobs:
  prettier:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: ${{ github.head_ref }}
      - name: Install
        run: npm ci
      - name: Run prettier
        run: npm run test:format
